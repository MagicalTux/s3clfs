detect and handle pressure from available hard disk space by removing oldest data

Handle HTTP errors (server unreachable, server error, etc) by retrying a bit later

Handle HTTP errors (access denied) by exiting and warning user

Create a control channel (unix socket?) and a management tool

filesystem check: clean inodes
=> Download full tree (all directory inodes)
=> Create list of all inodes in use
=> Delete any inode created before check started

filesystem check: unused hashes
=> create list of all hashes
=> download all file/dir inodes
=> remove all actually used hashes from previous list
=> delete anything remaining in the list

Add better configuration system (config file?)

Implement encryption and compression
